# 对照实验双包 + 强制 buffer=3（2026-07-28）

状态：`research_only_not_promoted`  
生产目录未改动；冻结钉仍指向 `15be8c14...`。

## 包定义

| 包 | 含义 | bundle / 指纹 | buffer | 可交易？ |
|----|------|---------------|--------|----------|
| **A** | 冻结规格 | `15be8c14...` / `887dfb45...` | 3 | **是** |
| **B** | 重搜索自动选择 | `3a9086f4...` / `3d94592e...` | 0 | **否** |
| **B3** | 同研究指纹强制 buffer=3 | 同 B 指纹 | 3（强制） | **否** |

## 核心结论

1. **只有包 A 可交易**  
2. **包 B** 败在全样本滚动 12 个月正超额 0.5688 < 0.60  
3. **即使强制 buffer=3（包 B3）也救不回来**  
   - 研究指纹下 buffer3 开发窗回撤 = **0.252626 > 0.25**  
   - 开发窗是全样本路径前缀 ⇒ 全样本最大回撤 **≥ 0.252626**  
   - 因此 `absolute_max_drawdown_at_most_0_25` 必然失败  
   - 不是“选错 buffer”，而是 **该指纹下 buffer3 本身已越线**

## 数字对照

### 全样本（官方落盘）

| 包 | buffer | IR | max_dd | roll12 | 批准 |
|----|--------|-----|--------|--------|------|
| A 冻结 | 3 | 0.527 | 0.2377 | 0.600 | 是 |
| B 重搜 | 0 | 0.413 | 0.2310 | 0.569 | 否 |

### 研究指纹强制扫描（开发窗官方候选 + 前缀下界）

| buffer | 开发窗 dd | 全样本 dd 下界 | 绝对回撤门(≤0.25) | 开发窗可入选 |
|--------|-----------|----------------|-------------------|--------------|
| 0 | 0.2262 | 0.2262 | 可能 | 是 |
| 1 | 0.2527 | 0.2527 | **否** | 否 |
| 2 | 0.2526 | 0.2526 | **否** | 否 |
| 3 | 0.2526 | 0.2526 | **否** | 否 |

## 方法说明

- 包 A/B：直接使用已晋升/已隔离的官方验收产物  
- 包 B3：在无法复现当日研究指纹行情缓存时，使用**路径前缀下界**  
  - `select_rotation_rank_buffer` 的 development = 全样本在 `holdout_start` 之前的前缀  
  - 同 buffer 全样本模拟在该前缀上的路径与开发窗模拟一致  
  - 故 `full_max_dd >= dev_max_dd`  
- 本地当前行情指纹仍是冻结指纹 `887dfb45...`，不能伪造研究指纹 `3d94592e...` 的点位回放

## 对生产的含义

- 继续钉死包 A  
- 包 B / B3 都只能留在隔离区  
- “重搜后强制改回 buffer3”**不是**恢复开仓的捷径  

机器可读：`dual_pack_comparison.json`
